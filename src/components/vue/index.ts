import STConnectionProfileSelect from './STConnectionProfileSelect.vue';
// Props are defined within the component script setup, but we can export them if needed for external typing.
// For now, consumers can use Vue's `ComponentProps` utility type: `import type { ComponentProps } from 'vue'; type Props = ComponentProps<typeof STConnectionProfileSelect>;`

export { STConnectionProfileSelect };